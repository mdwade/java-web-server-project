<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Default Page</title>
  </head>
  <body>
  	<table>
	    {% for item in items %}
	    	<tr>
	    		<td>
			    	<a href="{{ item }}">{{ item }}</a>
	    		</td>
	    	</tr>
	    {% endfor %}
  	</table>
  </body>
</html>
